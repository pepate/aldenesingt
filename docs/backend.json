{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information, including their role and usage limits.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the user, matching their auth UID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user profile was created."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the application.",
          "enum": ["admin", "creator", "user"]
        },
        "songsGeneratedToday": {
          "type": "number",
          "description": "The number of songs the user has generated today."
        },
        "lastGenerationDate": {
          "type": "string",
          "format": "date",
          "description": "The date (YYYY-MM-DD) of the last song generation."
        }
      },
      "required": ["id", "email", "role"]
    },
    "Song": {
      "title": "Song",
      "description": "Represents a song with lyrics and chords generated by AI.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who generated the song."
        },
        "creatorName": {
          "type": "string",
          "description": "The display name of the user who generated the song."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of generation."
        },
        "title": {
          "type": "string",
          "description": "The title of the song."
        },
        "artist": {
          "type": "string",
          "description": "The artist of the song."
        },
        "sheet": {
          "type": "object",
          "description": "The structured song sheet data from the AI.",
          "properties": {
            "releaseDate": {
              "type": "string"
            },
            "genre": {
              "type": "string"
            },
            "key": {
              "type": "string"
            },
            "song": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "part": {
                    "type": "string"
                  },
                  "lines": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "chords": {
                          "type": "string"
                        },
                        "text": {
                          "type": "string"
                        }
                      },
                      "required": ["chords", "text"]
                    }
                  }
                },
                "required": ["part", "lines"]
              }
            }
          },
          "required": ["song"]
        }
      },
      "required": ["userId", "createdAt", "title", "artist", "sheet"]
    },
    "Session": {
      "title": "Session",
      "description": "Represents a synchronized viewing session.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the session."
        },
        "hostId": {
          "type": "string",
          "description": "The user ID of the session host."
        },
        "songId": {
          "type": "string",
          "description": "The ID of the song document being viewed."
        },
        "scroll": {
          "type": "number",
          "description": "The current scroll position of the document."
        },
        "transpose": {
          "type": "number",
          "description": "The number of semitones to transpose the chords. Default is 0."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the session was created."
        },
        "lastActivity": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last interaction to enable TTL."
        }
      },
      "required": ["id", "hostId", "songId", "scroll", "createdAt", "lastActivity"]
    },
    "SessionParticipant": {
      "title": "Session Participant",
      "description": "Represents a user who has joined a session.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the participant entry, usually the user's UID."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who joined."
        },
        "sessionId": {
          "type": "string",
          "description": "The ID of the session they joined."
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user who joined."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the participant's profile picture."
        },
        "joinedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user joined the session."
        }
      },
      "required": ["id", "userId", "sessionId", "joinedAt"]
    }
  },
  "auth": {
    "providers": ["google.com", "password", "anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profiles for all users."
    },
    "/songs/{songId}": {
      "schema": {
        "$ref": "#/entities/Song"
      },
      "description": "A top-level collection for all publicly readable songs."
    },
    "/sessions/{sessionId}": {
      "schema": {
        "$ref": "#/entities/Session"
      },
      "description": "Contains data for a single collaborative session."
    },
    "/sessions/{sessionId}/sessionParticipants/{sessionParticipantId}": {
      "schema": {
        "$ref": "#/entities/SessionParticipant"
      },
      "description": "A subcollection tracking users who have joined a session."
    }
  }
}
