{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the user, matching their auth UID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user profile was created."
        }
      },
      "required": ["id", "email"]
    },
    "Song": {
      "title": "Song",
      "description": "Represents a song with lyrics and chords extracted from a document.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the song."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who uploaded the song."
        },
        "title": {
          "type": "string",
          "description": "The title of the song."
        },
        "content": {
          "type": "string",
          "description": "The extracted lyrics and chords, formatted for display."
        },
        "storagePath": {
          "type": "string",
          "description": "The path to the original file in Firebase Storage."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the song was created."
        }
      },
      "required": ["id", "userId", "title", "content", "createdAt"]
    },
    "Session": {
      "title": "Session",
      "description": "Represents a synchronized viewing session.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the session."
        },
        "hostId": {
          "type": "string",
          "description": "The user ID of the session host."
        },
        "songId": {
          "type": "string",
          "description": "The ID of the song document being viewed."
        },
        "scroll": {
          "type": "number",
          "description": "The current scroll position of the document."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the session was created."
        }
      },
      "required": ["id", "hostId", "songId", "scroll", "createdAt"]
    },
    "SessionParticipant": {
      "title": "Session Participant",
      "description": "Represents a user who has joined a session.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the participant entry, usually the user's UID."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who joined."
        },
        "sessionId": {
          "type": "string",
          "description": "The ID of the session they joined."
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user who joined."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the participant's profile picture."
        },
        "joinedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user joined the session."
        }
      },
      "required": ["id", "userId", "sessionId", "joinedAt"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profiles for all users."
    },
    "/songs/{songId}": {
      "schema": {
        "$ref": "#/entities/Song"
      },
      "description": "A top-level collection for all publicly readable songs."
    },
    "/sessions/{sessionId}": {
      "schema": {
        "$ref": "#/entities/Session"
      },
      "description": "Contains data for a single collaborative session."
    },
    "/sessions/{sessionId}/sessionParticipants/{sessionParticipantId}": {
      "schema": {
        "$ref": "#/entities/SessionParticipant"
      },
      "description": "A subcollection tracking users who have joined a session."
    }
  }
}
