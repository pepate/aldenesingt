# This file is a template for environment variables.
# These variables can be used for non-secret configuration that is consistent across all environments.
# For local development secrets, use .env.local (see .env.local.template).
